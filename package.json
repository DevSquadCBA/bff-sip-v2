{
  "dependencies": {
    "mysql2": "^3.1.2"
  },
  "devDependencies": {
    "aws-lambda": "^1.0.7",
    "typescript": "^5.2.2",
    "@types/aws-lambda": "^8.10.125",
    "@types/node": "^18.16.13",
    "yaml": "^2.3.3"
  },
  "scripts": {
    "start": "( Write-Output 'Powershell' && ./scripts/local_invoke.bat ) || ( CALL ./scripts/local_invoke.bat ) || ( bash -c 'uname -a | grep -q -i Linux' && bash -c ./scripts/local_invoke.sh ) ||( bash -c 'uname -a | grep -q -i Darwin' && bash -c ./scripts/local_invoke.sh )",
    "test": "npm run coverage"
  }
}
